# Exemple de fichier .env pour le backend Caretime

# URL de connexion à MongoDB (local ou distant)
MONGO_URI=mongodb://localhost:27017/clinic

# Clé secrète pour la génération des tokens JWT
JWT_SECRET=supersecretkey

# Port d'écoute du serveur Express (optionnel)
PORT=5000